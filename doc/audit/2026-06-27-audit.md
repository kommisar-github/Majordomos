# Fleet Audit — 2026-06-27

**Trigger:** `/pm audit` immediately after applying errata v4.24 (Stay in Your Lane)
and v4.25 (Specialist-Owned Dynamic Workflows). Read-only diagnostic; no fixes applied.

## Roster at audit time

Online (green): `pm`, `arch`, `review`, `scm`, `hw_lib`, `telegram` (bridge).
Offline (not launched this session): `app`, `ha`, `ops`, `ha_devops`,
`swarm`, `dragon-vlm`, `jetson-protect`. Federation bridges are offline by design.

Audit directive dispatched to the 4 dispatchable online specialists (arch, review, scm, hw_lib).

## Phase 1 — Agent self-reports (4/4 responded)

| Agent | skill_sha256 vs disk | guidelines_sha256 vs disk | stray_memory | complaints | version_seen |
|-------|----------------------|---------------------------|--------------|------------|--------------|
| arch    | ✅ match | ✅ match | none | none | v4.23 ⚠️ |
| review  | ✅ match | ✅ match | none | none | v4.23 ⚠️ |
| scm     | ✅ match | ✅ match | none | none | v4.23 ⚠️ |
| hw_lib  | ✅ match | ✅ match | none | none | v4.23 ⚠️ |

(`in_flight_task_id` for arch/review/hw_lib was each agent's own audit task — benign.)

## Phase 2 — PM-local cross-checks

| Check | Result |
|-------|--------|
| (3) `## Memory Policy` block in every specialist SKILL | ✅ 11/11 present |
| (4) `## MCP Transport` block in every specialist SKILL | ✅ 11/11 present |
| (5) stray_memory_files (W2) | ✅ none reported |
| (6) `doc/<agent>_GUIDELINES.md` present | ✅ 11/11 present |
| (7) runtime-artifact drift | ✅ client `node/v4.6` == server `expected_client_protocol` `node/v4.6`; `stale_clients: []`; `start.sh` present |
| (8) hook integrity | ✅ SessionStart (start.sh + /memories?agent= + MEMORY.md), UserPromptSubmit (/hook/check), Stop + StopFailure (/hook/stop) — all present, all wrapped in `{hooks:[]}`, no flat entries |

## Findings

**1. Stale in-context SKILLs (informational, expected).** All live terminals report
`version_seen: v4.23`. They launched before this session's errata bump (seed-state is now
v4.25 on disk). Their on-disk `sha256sum` matches because the audit re-reads disk — but the
SKILL content **loaded into each terminal's context** is the pre-erratum v4.23 copy. The
v4.24 "Stay in Your Lane" and v4.25 "Dynamic Workflows" behaviors will not be active in those
agents until their terminals **restart and reload the SKILL**.

**Recommendation:** restart the agent terminals (arch, review, scm, hw_lib) — and PM itself —
to pick up the v4.24/v4.25 SKILL changes. No re-seed needed (runtime artifacts are current;
this is in-context staleness, not bundle drift).

**2. No structural drift.** Runtime artifacts current, hooks well-formed, all canonical
blocks and GUIDELINES docs present. The errata-apply left no hook/artifact/seed-version
mismatch the channel can't fix.

**3. Coverage gap (scope note).** 7 of 11 specialists were offline, so this audit only
self-reported 4. Disk-side checks (blocks, guidelines, hashes) covered all 11.
