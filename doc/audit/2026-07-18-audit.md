# Fleet Audit — 2026-07-18

**Command:** `/pm audit` (read-only diagnostic; no fixes applied)
**Trigger:** post-apply verification after landing errata chain v4.33 → v4.39 (seed_version 4.32 → 4.39).
**Scope:** online agents only. `pm` + `scm` audited. `app`, `ha`, `ops`, `ha_devops`, and the 3 federation bridges (`swarm`, `dragon-vlm`, `jetson-protect`) were **offline** — not covered.

## PM-local runtime / structural checks

| # | Check | Result | Detail |
|---|-------|--------|--------|
| 6 | Runtime-artifact drift (`stale_runtime_artifacts`) | ✅ clean | Server 2.9.1 `expected_client_protocol=node/v4.6`; local `client.js` `PROTOCOL_VERSION=node/v4.6` → match. `start.sh` present. |
| 7 | Hook integrity (`hook_integrity`) | ✅ clean | `SessionStart` = start.sh + agent-memory + repo-memory; `UserPromptSubmit` = `/hook/check`; `Stop` + `StopFailure` = `/hook/stop`. All entries use `{ "hooks": [...] }` envelope; no flat entries. PreCompact/PostCompact/compact-restore also wired. |
| — | seed_version vs templates | ✅ clean | `seed-state.json`=4.39, `PM.md`=4.39, `PM_TEMPLATES.md`=4.39. |
| — | Working tree | ✅ clean | Only `task-router.db` dirty (runtime artifact). Commit `e9fb4c8` on `origin/main`. |

## Per-agent findings

| Agent | skill SHA match | GUIDELINES | MCP Transport | Memory Policy | Code Review | Stray memory | In-flight | version_seen | Complaints |
|-------|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|
| scm | ✅ match | ✅ present | ✅ | ✅ | ✅ | none | null | v4.39 | none |

- `scm` `skill_sha256` = `dbd8f78a…` matches disk → terminal running current SKILL.md (not stale).
- `scm` `guidelines_sha256` = `ac9fd195…` matches disk → `doc/scm_GUIDELINES.md` present and consistent.
- `version_seen=v4.39` matches current seed.

## Verdict

No drift found on the online fleet. The errata chain v4.33→v4.39 landed cleanly — no stale runtime artifact, no seed-vs-templates mismatch, no hook damage. **No action required.**

## Not covered (offline)

`app`, `ha`, `ops`, `ha_devops`, `swarm`, `dragon-vlm`, `jetson-protect`. Re-run `/pm audit` after launching those terminals for full-roster coverage.
