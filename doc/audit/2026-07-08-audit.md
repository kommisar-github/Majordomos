# Fleet Audit — 2026-07-08

**Trigger:** post-errata (`/pm audit` after applying v4.27–v4.31).
**Scope:** local fleet. Phase 1 (live agent self-report) **skipped** — only `pm` + `telegram`
were registered; the 8 specialists + 3 federation bridges were offline (no terminals to answer).
This is a **Phase-2 PM-local disk cross-check** only.

## Roster at audit time

| Agent | State | Notes |
|-------|-------|-------|
| pm | 🟢 green | this terminal |
| telegram | 🟢 green | bridge (pid 5024) |
| app, ha, ops, review, scm, arch, hw_lib, ha_devops | ⚪ offline | not launched |
| swarm, dragon-vlm, jetson-protect | ⚪ offline | federation bridges (offline by design) |

## Findings

| # | Check | Result |
|---|-------|--------|
| 1 | **Seed version** | `seed_version = 4.31`, == active errata max. All 5 pending errata applied + acked. ✅ current |
| 2 | **Canonical SKILL blocks** (MCP Transport / Memory Policy / Code Review) | All 8 code specialists (app, ha, ops, review, scm, arch, hw_lib, ha_devops) carry all three. ✅ |
| 3 | **Code Review block — federation bridges** | swarm, dragon-vlm, jetson-protect LACK `## Code Review`. **By design** — they are relay-only `federated-pm` bridges, non-code, excluded from the v4.31 code gate (verification scope: "code-producing specialist"). ⚠️ soft — see note |
| 4 | **Runtime-artifact drift** | client `PROTOCOL_VERSION = node/v4.6` == server `expected_client_protocol = node/v4.6`; `start.sh` present. ✅ no drift |
| 5 | **Hook integrity** | SessionStart (3 command hooks: start.sh + /memories + memory/MEMORY.md), UserPromptSubmit (/hook/check), Stop + StopFailure (/hook/stop), **PreCompact (/hook/precompact) + PostCompact (/hook/compacted)** all present, well-formed envelopes, no flat entries. ✅ v1.8.0 compaction hooks already installed |
| 6 | **Stray memory files** | Only canonical `memory/` (8 files). No `.claude/memory/`, no harness auto-memory dir. ✅ |

## Note on finding 3

The v4.31 audit rule reads "flags absence of `## Memory Policy` **or** `## Code Review` block in
any specialist SKILL." Taken literally, the 3 federation bridges would flag. But the v4.31
erratum scoped the `## Code Review` block to **code-producing** specialists (those with
`## Result Discipline`); the bridges are relay-only and produce no code, so their absence is
**expected, not drift**. Options: (a) leave exempt (recommended — matches erratum intent), or
(b) add an inert `## Code Review` note to the bridges for literal audit-cleanliness.

## Verdict

**No RE-SEED required.** Runtime artifacts and hooks (incl. compaction) are current for this
Claude-only fleet. The v4.29/v4.30 local-model runtime artifacts (`local-runner.js`,
`harness-runner.js`) are irrelevant here — no `backend`/`harness` agent exists. All errata-delivered
doc/orchestration changes are on disk and verified; they remain **uncommitted** in the working tree.
